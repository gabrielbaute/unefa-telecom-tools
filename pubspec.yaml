name: unefa_telecom_tools
description: "Una app de utilería para el estudiante de Telecomunicaciones de la UNEFA."
publish_to: 'none'
version: 0.4.1+6

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.3.0
  package_info_plus: ^10.1.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
